[package]
name = "tcp-bridge"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
clap = { version = "4.0", features = ["derive"] }
hex = "0.4"
socket2 = "0.5" # For binding to specific source IP
byteorder = "1.4"
